/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ["localhost", "127.0.0.1"], // Add both localhost and 127.0.0.1
    },
  };
  
  export default nextConfig;
  